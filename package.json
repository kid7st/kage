{
  "name": "kage",
  "version": "0.1.0",
  "description": "Shadow-clone a git repo into an isolated folder, drop into pi to work in parallel, then merge the session memory back",
  "keywords": ["pi", "coding-agent", "git", "parallel", "worktree", "cli"],
  "license": "MIT",
  "type": "module",
  "bin": {
    "kage": "./bin/kage.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
